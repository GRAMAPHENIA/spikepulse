/**
 * Gestor de entrada - Placeholder para futuras implementaciones
 * @module InputManager
 */

export class InputManager {
    constructor(config, eventBus) {
        this.config = config;
        this.eventBus = eventBus;
        console.log('🎮 InputManager placeholder creado');
    }
    
    init() {
        // Implementación en tareas futuras
    }
    
    update(deltaTime) {
        // Implementación en tareas futuras
    }
    
    destroy() {
        // Implementación en tareas futuras
    }
}